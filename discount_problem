bool discount(String status, int purchase){
  if(status == 'premium' || purchase > 5000){
    return true;
  }
  return false;
}

void main(){
  if(discount('premium', 400)){
    print('Discount available');
  } else {
    print('You are not eligible for discount');
  }
}
